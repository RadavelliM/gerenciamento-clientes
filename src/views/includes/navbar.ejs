<nav class="navbar navbar-expand navbar-dark bg-dark" id="navbar">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Gerenciamento</a>

        <% if(user) { %>
        <h3 class="navbar mb-auto ms-5 text-danger"><%= user.nome %></h3>
        <% } %>

        <button class="navbar-toggler" type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent"
            aria-expanded="false"
            aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">

                <% if(user) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/login/logout">Desconectar</a>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Entrar</a>
                    </li>
                <% } %>

                <li class="nav-item">
                    <a class="nav-link" href="/createAccount">Criar conta</a>
                </li>

                <% if(user) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/contato">Cadastrar contato</a>
                    </li>
                <% } %>

            </ul>
        </div>
    </div>
</nav>
